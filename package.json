{
  "name": "caisse-app",
  "version": "1.0.0",
  "description": "Application de gestion de caisse",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand --json --outputFile=test-results.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3"
  }
}
