# Projet Caisse

## Structure du projet

- `src/` : logique métier (ex: changeLogic.js)
- `public/` : fichiers statiques pour le front (index.html)
- `scripts/` : scripts front-end JS
- `tests/` : tous les tests unitaires, fonctionnels, E2E
- `server.js` : serveur Node.js Express
- `package.json` : configuration npm

## Lancer le projet

```bash
npm install
node server.js
```

## Lancer les tests

```bash
npx jest
```
